[Link to UI](https://uladbolatau.github.io/angular/)

# Before start 
## Setup husky
```
husky install && npx husky add .husky/pre-commit 'npm run lint'
```
## Add dist direcotry
```
npx angular-cli-ghpages --dir=dist/<repo name>
```
